{
  "name": "@hearkus/trpc-api",
  "version": "0.1.0",
  "private": true,
  "description": "Type-safe API implementation built on tRPC for @hearkus/nextjs.",
  "license": "AGPL-3.0",
  "scripts": {
    "clean": "rm -rf .next .turbo node_modules",
    "lint": "eslint --config ../../.eslintrc.json src/**/*.ts"
  },
  "dependencies": {
    "zod": "^3.20.2"
  },
  "devDependencies": {
    "@types/node": "^18.11.18",
    "dotenv-cli": "^6.0.0"
  }
}
